"use client";

import { ContactSectionFAQ } from "../faq/ContactSectionFAQ";

export function ContactSectionNoticias() {
  // Reuse the exact same component from FAQ to keep visual parity
  return <ContactSectionFAQ />;
}